export enum CryptrReducerActionKind {
  LOADING = 'loading',
  UNLOADING = 'unloading',
  AUTHENTICATED = 'authenticated',
  UNAUTHENTICATED = 'unauthenticated',
  ERROR = 'error',
}

export enum Locale {
  EN = 'en',
  FR = 'fr',
  // GB = 'gb',
}
